### Prompt 模板

---

#### 性格词汇量化值Prompt

**目标：** 分析并量化提供的性格词汇，为每个词汇分配一个数值范围内的值，以此来衡量该性格特征的强度或表现程度。

**输入格式：**

```
性格词汇: [词汇1], [词汇2], ..., [词汇N]
量化范围: [最小值] - [最大值]
量化说明: 请为每个性格词汇分配一个数值，该数值应在指定的范围内，反映该性格特征的强度或表现程度。数值越高表示该性格特征越明显或强烈；数值越低则相反。

示例词汇:
- 开放性
- 责任心
- 外向性
- 宜人性
- 神经质
```

**输出格式：**

```
{
  "[词汇1]": [数值],
  "[词汇2]": [数值],
  ...
  "[词汇N]": [数值]
}
```

---

#### 示例

**输入：**

```
性格词汇: 开放性, 责任心, 外向性
量化范围: 1 - 10
量化说明: 请为每个性格词汇分配一个1到10之间的数值，1表示该性格特征几乎不明显，10表示该性格特征极其明显。
```

**输出：**

```
{
  "开放性": 7,
  "责任心": 9,
  "外向性": 5
}
```
我给你提供真实输入，你只需要返回我输出的json即可
**真实输入：**

```
性格词汇: {attribute}
量化范围: 1 - 10
量化说明: 请为每个性格词汇分配一个1到10之间的数值，1表示该性格特征几乎不明显，10表示该性格特征极其明显。
```